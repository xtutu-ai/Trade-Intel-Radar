services:
  trade-intel-radar:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    env_file:
      - ../.env
    volumes:
      - ../data:/app/data
      - ../logs:/app/logs
    command: ["python", "scripts/run_daily.py"]
